# 💰 Smart Cash Flow OCR - คู่มือการติดตั้ง (Installation Guide)

คู่มือนี้จะช่วยให้คุณติดตั้งและใช้งานโปรแกรม OCR สำหรับแยกข้อมูลจากเอกสารธนาคารได้อย่างง่ายดาย

## 📋 สิ่งที่ต้องเตรียม (Prerequisites)

เพื่อให้โปรแกรมทำงานได้ คุณต้องติดตั้งโปรแกรมเสริมดังนี้:

1.  **Python 3.10+**: [ดาวน์โหลดได้ที่นี่](https://www.python.org/downloads/)
    *   *สำคัญ:* ตอนติดตั้งอย่าลืมติ๊กถูกที่ **"Add Python to PATH"**
2.  **Tesseract OCR**: [ดาวน์โหลดตัวติดตั้งสำหรับ Windows (.exe)](https://github.com/UB-Mannheim/tesseract/wiki)
    *   ติดตั้งลงเครื่อง (ปกติจะอยู่ที่ `C:\Program Files\Tesseract-OCR`)
3.  **Poppler**: [ดาวน์โหลดได้ที่นี่](https://github.com/oschwartz10612/poppler-windows/releases/)
    *   แตกไฟล์ zip และเก็บไว้ในโฟลเดอร์ที่คุณต้องการ (เช่น `C:\poppler`)
    *   โปรแกรมนี้ใช้สำหรับแปลงไฟล์ PDF เป็นภาพ

---

## 🚀 ขั้นตอนการติดตั้ง (Setup Steps)

### 1. ดาวน์โหลดโปรแกรม
ดาวน์โหลดหรือ Clone โปรเจคนี้ลงในเครื่องคอมพิวเตอร์ของคุณ

### 2. ตั้งค่าคอนฟิก (Configuration)
เปิดไฟล์ `config.json` และแก้ไขพาธ (Path) ให้ตรงกับที่ติดตั้งในเครื่องของคุณ:

```json
{
  "API_KEY": "ใส่ API Key ของคุณที่นี่ (ถ้ามี)",
  "TESSERACT_PATH": "C:\\Program Files\\Tesseract-OCR\\tesseract.exe",
  "POPPLER_PATH": "C:\\poppler\\Library\\bin"
}
```

### 3. ติดตั้ง Dependencies
ดับเบิลคลิกที่ไฟล์ `setup_env.bat`
*   โปรแกรมจะสร้าง Virtual Environment (`.venv`)
*   ติดตั้งไลบรารีที่จำเป็นโดยอัตโนมัติ

---

## 🏃 วิธีการใช้งาน (How to use)

1.  นำไฟล์ PDF ของธนาคารที่ต้องการประมวลผลไปวางในโฟลเดอร์ `source`
2.  ดับเบิลคลิกที่ไฟล์ `run_app.bat`
3.  โปรแกรมจะเปิดหน้าต่างเบราว์เซอร์ขึ้นมา (Streamlit)
4.  กดปุ่ม **"Scan Source Folder"** เพื่อค้นหาไฟล์
5.  เลือกไฟล์ที่ต้องการและกด **"Run OCR"**

---

## 🛠️ การแก้ไขปัญหาเบื้องต้น (Troubleshooting)

*   **โปรแกรมหา Tesseract ไม่เจอ:** ตรวจสอบพาธใน `config.json` ว่าถูกต้องและใช้ backslash สองตัว (`\\`) หรือ slash ตัวเดียว (`/`)
*   **เปิด PDF ไม่ได้:** ตรวจสอบว่า `POPPLER_PATH` ใน `config.json` ชี้ไปที่โฟลเดอร์ `bin` ของ Poppler
*   **ติดตั้ง Library ไม่ผ่าน:** ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต และลองลบโฟลเดอร์ `.venv` แล้วรัน `setup_env.bat` ใหม่
